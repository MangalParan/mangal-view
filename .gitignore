__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
*.egg-info/
dist/
build/
bench_results.txt
*.db
# Runtime trade log generated by the AI Bot panel (contains live order IDs,
# fill prices, timestamps — kept out of source control).
log.txt
# Strategy Menu trade log (Zerodha + Delta sides) — same reason as log.txt
log_options.txt
# Bot session logs + the journal workbook derived from them (same live trade data)
logs/
Journal.xlsx
# Local dev server output
server.log
# Persisted Zerodha/Kite session (access token — secret, never commit)
zerodha_session.json
# Persisted Telegram bot token + chat_id (secret, never commit)
telegram_config.json
# Third-party TradingView Pine scripts kept locally for reference (not app code)
*.pine
# Nifty_MV_bot Telegram message log (viewable/downloadable from the panel)
telegram.txt
